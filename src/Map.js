import { Box } from "@mui/material";

function Map() {
    return (
        <Box id='map' sx={{flexGrow: 1, backgroundColor: 'blue'}}>
        this is the map
        </Box>
    );
  }
  
  export default Map;
  